using Xunit;

// Случайность в мире — единый глобальный поток (см. Core.Rng), и тесты,
// задающие ему зерно, неизбежно мешали бы друг другу, идя параллельно:
// два класса, одновременно вызвавших Rng.Initialize, получают чужие броски.
// Поэтому весь набор идёт последовательно — он всё равно проходит за секунду
[assembly: CollectionBehavior(DisableTestParallelization = true)]
