namespace Aeternum.WorldGen.Models;

// Возрастная группа для статистики (StatisticsSystem) — по порогам
// WorldSettings.AdultAge/ElderAge, не путать с LifeStage (игровые правила)
public enum AgeGroup
{
    Child,
    Adult,
    Elder
}
